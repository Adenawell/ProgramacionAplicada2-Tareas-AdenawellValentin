datasource db {
  provider = "postgresql"
  schemas  = ["carrito"]
}

generator client {
  provider = "prisma-client-js"
}

model Producto {
  id       Int    @id @default(autoincrement())
  nombre   String @unique
  precio   Float
  cantidad Int

  @@schema("carrito")
}