datasource db {
  provider = "postgresql"
  schemas  = ["turnos"]
}

generator client {
  provider        = "prisma-client-js"
}

model Turno {
  id       Int    @id @default(autoincrement())
  cliente  String
  servicio String
  estado   String @default("esperando")

  @@schema("turnos")
}