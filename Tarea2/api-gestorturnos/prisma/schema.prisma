datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model Turno {
  id       Int    @id @default(autoincrement())
  cliente  String
  servicio String
  estado   String @default("esperando")
}